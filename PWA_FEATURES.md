# PWA 功能文檔

## 概述

本應用已升級為漸進式網頁應用程式 (Progressive Web App, PWA)，提供類似原生應用的體驗。

## 主要功能

### 1. 可安裝應用程式

#### 什麼是 PWA 安裝？
- 將網頁應用安裝到設備主屏幕
- 像原生應用一樣啟動，無需瀏覽器界面
- 獨立的應用圖標和啟動畫面

#### 如何安裝？

**桌面瀏覽器 (Chrome, Edge):**
1. 訪問時間表網頁
2. 點擊地址欄右側的安裝圖標 ⊕
3. 點擊「安裝」確認
4. 應用將添加到桌面/應用程式列表

**iOS Safari:**
1. 訪問時間表網頁
2. 點擊分享按鈕 (⬆️)
3. 選擇「加入主屏幕」
4. 輸入名稱並確認

**Android Chrome:**
1. 訪問時間表網頁
2. 點擊頁面底部彈出的「安裝」提示
3. 或點擊選單 → 「安裝應用程式」
4. 確認安裝

### 2. 離線支援

#### 快取策略
應用使用「快取優先」策略：
- 首次訪問時下載並快取所有資源
- 後續訪問優先使用快取（即時加載）
- 背景更新快取確保資料最新
- 完全離線時仍可查看上次載入的時間表

#### 離線功能
✅ 查看時間表
✅ 切換日期
✅ 查看當前課程
✅ 倒計時功能
❌ 需要網絡連接時會顯示提示

#### 離線指示器
- 🟢 **已連線** - 正常網絡連接
- 🔴 **離線模式** - 無網絡連接，使用快取資料

### 3. 課程通知系統

#### 背景通知
即使關閉網頁，Service Worker 也能在背景執行並發送通知。

#### 通知功能
- **自動排程**：每天首次打開應用時自動排程當日課程通知
- **提前提醒**：每堂課前 5 分鐘發送通知
- **詳細資訊**：顯示課程名稱、時間和科目
- **智能管理**：自動識別新的一天並重新排程

#### 如何啟用通知？

1. 點擊頂部的通知按鈕 (🔔)
2. 瀏覽器會請求通知權限
3. 點擊「允許」
4. 系統自動排程今日所有課程通知

#### 通知按鈕狀態
- 🔔 (半透明) - 未啟用，點擊啟用
- 🔔 (實心) - 已啟用
- 🔕 - 權限被拒絕，需要在瀏覽器設定中手動允許

#### 通知範例
```
標題：第1節課程即將開始
內容：MATH YPC 405 (08:40 - 09:20)
```

### 4. 性能優化

#### 加載速度優化
- **快取優先策略**：第二次訪問幾乎瞬間加載（< 100ms）
- **背景更新**：在使用快取版本的同時背景更新資源
- **資源預加載**：Service Worker 預先快取關鍵資源

#### 用戶體驗優化
- **自動滾動**：自動滾動到當前課程
- **平滑動畫**：使用 CSS 過渡動畫
- **骨架屏**：載入時顯示佔位符而非空白
- **響應式設計**：適配所有設備尺寸

### 5. Service Worker 技術細節

#### 生命週期
```
安裝 → 激活 → 運行 → 更新
```

#### 快取管理
- **版本控制**：`timetable-v1`
- **自動清理**：刪除舊版本快取
- **資源清單**：
  - `/` 或 `/index.html`
  - `/timetable-data.js`
  - `/manifest.json`

#### 通知隊列
- Service Worker 維護通知隊列
- 每個通知有唯一 ID：`{日期}-period-{節數}`
- 自動管理 setTimeout 防止內存洩漏
- 支援取消和查詢通知狀態

## 技術架構

### 文件結構
```
/
├── index.html          # 主頁面
├── manifest.json       # PWA 配置文件
├── sw.js              # Service Worker
└── timetable-data.js  # 時間表數據
```

### 關鍵技術

#### 1. Web App Manifest
定義應用的外觀和行為：
- 名稱和圖標
- 主題顏色
- 啟動模式（standalone）
- 顯示方向（portrait）

#### 2. Service Worker
背景腳本，提供：
- 離線支援
- 快取管理
- 背景通知
- 資源預加載

#### 3. Notification API
Web 標準通知接口：
- 請求權限
- 發送通知
- 處理點擊事件
- 自定義樣式

#### 4. Cache API
存儲和檢索資源：
- 快取策略
- 版本管理
- 背景更新

## 瀏覽器支援

| 功能 | Chrome | Firefox | Safari | Edge |
|------|--------|---------|--------|------|
| PWA 安裝 | ✅ | ✅ | ✅ | ✅ |
| Service Worker | ✅ | ✅ | ✅ | ✅ |
| 推送通知 | ✅ | ✅ | ⚠️ | ✅ |
| 離線快取 | ✅ | ✅ | ✅ | ✅ |
| 背景同步 | ✅ | ❌ | ❌ | ✅ |

⚠️ Safari iOS 支援通知，但需要將應用加入主屏幕後才能使用

## 最佳實踐

### 用戶體驗
1. **首次訪問**
   - 允許通知權限以獲得最佳體驗
   - 將應用加入主屏幕

2. **日常使用**
   - 應用會自動排程當天課程通知
   - 離線時仍可查看時間表
   - 定期打開應用以更新資料

3. **通知管理**
   - 如不需要通知，可在系統設定中關閉
   - 通知會在每天首次打開時自動更新

### 開發者注意事項
1. **Service Worker 更新**
   - 修改 `CACHE_NAME` 版本號以觸發更新
   - 用戶刷新頁面後會自動更新

2. **通知調試**
   - 使用瀏覽器開發者工具 → Application → Service Workers
   - 檢查 Console 日誌查看通知排程狀態

3. **快取管理**
   - 在開發者工具中可手動清除快取
   - Application → Cache Storage → 刪除相關快取

## 故障排除

### 問題：通知沒有發送
**解決方案：**
1. 檢查通知權限是否允許
2. 確認 Service Worker 已註冊
3. 查看 Console 是否有錯誤
4. 重新點擊通知按鈕重新排程

### 問題：應用無法離線使用
**解決方案：**
1. 確保至少在線訪問過一次
2. 檢查 Service Worker 是否正常運行
3. 清除快取後重新訪問

### 問題：無法安裝 PWA
**解決方案：**
1. 確認使用支援的瀏覽器
2. 檢查是否使用 HTTPS（本地開發可用 localhost）
3. 確認 manifest.json 和 Service Worker 正確載入

### 問題：通知在 iOS 上不工作
**解決方案：**
1. 確認已將應用加入主屏幕
2. iOS Safari 需要從主屏幕圖標啟動應用
3. 檢查 iOS 系統通知設定

## 未來改進

### 計劃功能
- [ ] 自定義通知時間（如課前 10 分鐘）
- [ ] 通知聲音選擇
- [ ] 每週時間表總覽
- [ ] 課程筆記功能
- [ ] 作業提醒
- [ ] 與日曆應用同步

### 性能優化
- [ ] 圖片資源優化
- [ ] 代碼分割和懶加載
- [ ] IndexedDB 存儲大量資料
- [ ] 更智能的快取策略

## 反饋與支持

如遇到問題或有改進建議，請通過以下方式聯繫：
- GitHub Issues
- 電郵支持
- 用戶反饋表單

---

最後更新：2026-02-23
版本：1.0.0
